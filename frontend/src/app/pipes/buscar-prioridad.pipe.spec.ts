import { BuscarPrioridadPipe } from './buscar-prioridad.pipe';

describe('BuscarPrioridadPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarPrioridadPipe();
    expect(pipe).toBeTruthy();
  });
});
